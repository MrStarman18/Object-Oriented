bool done = false;
while(!done){  //"while not done"
    //do stuff
    //decide if we're done
    if (condition)
        done = true
    //do more stuff
}
//Booleans assigned as variable values, like int.

/*Punctuation Names
! bang (not)
~ twiddle (not)
@ at
# pound
$ dollar
% percent
^ carrot
& and
* star, splat
()
[] square brackets
{} squiggle brackets
_ under
- minus
+ plus
= equal
| pipe
\ backslash/bash
" double quote
' single quote (with double)
` back quote (with twiddle)
? hook
: colon
; semicolon/semi
< less than
> greater than